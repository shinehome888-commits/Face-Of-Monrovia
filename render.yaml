services:
  - type: web
    name: face-of-monrovia
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    nodeVersion: "18"
    envVars:
      - key: NODE_ENV
        value: production
